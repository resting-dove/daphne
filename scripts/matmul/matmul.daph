N = 120;
//A = rand(N, N, 0.0, 5.0, 1.0, 42);
//B = rand(N, N, 0.0, 3.0, 1.0, 43);
A = fill(5.0, N, N);
B = fill(3.0, N, N);

start = now();
C = A@B;
end = now();

print("GEMM took ", 0);
print((end-start)/1000000.0, 0);
print("ms", 1);
//print(C); // for small matrices
print(C[0,0]); // if don't want to print large matrices